plugins {
    id 'base'
    alias(libs.plugins.axion)
}

allprojects {
    project.version = scmVersion.version
    project.group = 'us.schueler.howto'
}

subprojects {
    apply plugin: 'base'
    archivesBaseName = "how-${it.name}"
}